import * as zod from "zod";

// Custom schema for route params that have a tutorId parameter
// export const tutorIdParamSchema = zod.z.object({
//     tutorId: zod.z.string().regex(/^\d+$/, "ID phải là một số").transform(Number),
// });
